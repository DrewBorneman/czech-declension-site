// import type { TProject } from "./components";

// I guess only for when the type that the API is returning is different than the type we need?
// export type TSiteInfo = {
//     name: string;
//     iconPath: string;
//     copyrightStatement: string;
//     passwordHash: string;
//     links: TSocialMediaLink[];
// }
